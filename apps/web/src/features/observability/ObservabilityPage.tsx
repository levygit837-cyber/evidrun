import { InlineNotice } from "../../ui/primitives";

export function ObservabilityPage() {
  return (
    <section className="feature-placeholder" aria-label="Observability">
      <InlineNotice title="Observability em preparação">A inspeção de Runs será carregada aqui.</InlineNotice>
    </section>
  );
}
