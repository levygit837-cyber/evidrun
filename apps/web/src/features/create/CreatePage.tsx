import { InlineNotice } from "../../ui/primitives";

export function CreatePage() {
  return (
    <section className="feature-placeholder" aria-label="Create">
      <InlineNotice title="Create em preparação">O fluxo Study → RunSpecs → Admission → Runs será carregado aqui.</InlineNotice>
    </section>
  );
}
