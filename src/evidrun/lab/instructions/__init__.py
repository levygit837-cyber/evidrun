"""Composição da instrução de sistema do Lab Agent.

Importe do submódulo que possui o símbolo: `compose` para montar, `base` para a base
invariante, `scope_blocks` para os três blocos e `derived` para o bloco gerado do runtime.
"""
